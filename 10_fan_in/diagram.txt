Fan-In (results aggregation)
   ┌────────────┐
   │  Worker 1  │------+
   │  (result)  │      │
   └────────────┘      │
   ┌────────────┐      │     ┌──────────────┐
   │  Worker 2  │------+---->│   Merger     │
   │  (result)  │      │     │  (fan-in)    │
   └────────────┘      │     │              │
   ┌────────────┐      │     │ [ ][ ][ ][ ] │
   │  Worker N  │------+     └──────┬───────┘
   │  (result)  │                   │ merged
   └────────────┘                   ▼
                               ┌──────────┐
                               │  Output  │
                               │  stream  │
                               └──────────┘

─────────────────────────────────────────
Сложность: ⭐⭐
Syscalls:  fork(), shmget(), shmat(), shmdt(), shmctl()
Real world: MapReduce (reduce), Kafka -> aggregator, Flink windowing
