Supervisor (супервизор)
   ┌──────────────────────────────────────┐
   │            Supervisor                │
   │  monitor --> restart --> healthcheck │
   │  ┌─ restart strategies: ──────────┐  │
   │  │ one_for_one                    │  │
   │  │ one_for_all                    │  │
   │  │ rest_for_one                   │  │
   │  │ simple_one_for_one             │  │
   │  └────────────────────────────────┘  │
   └──────┬──────────────┬──────────────┬─┘
          │ spawn        │ monitor      │ stop
     ┌────▼────┐    ┌────▼────┐    ┌────▼────┐
     │ Worker  │    │ Worker  │    │ Worker  │
     │   A     │    │   B     │    │   C     │
     │  run    │    │  run    │    │  run    │
     │   ||    │    │   ||    │    │   ||    │
     └────┬────┘    └────┬────┘    └────┬────┘
          │              │              │
          └──────────────┴──────────────┘
                         │
                         ▼
                  Supervisor restarts workers
                     worker comes back

─────────────────────────────────────────
Сложность: ⭐⭐⭐⭐
Syscalls:  fork(), waitpid() (WNOHANG), kill(), signal()
Real world: supervisord, systemd, PM2, Erlang/OTP supervisor, PHP-FPM master
