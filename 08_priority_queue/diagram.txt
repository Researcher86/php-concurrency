Priority Queue (priority queue)
        ┌────────────────────┐
        │   Incoming Tasks   │
        │  [T1][T2][T3][T4]  │
        └─────────┬──────────┘
                  │ ordering
                  │ by priority
                  ▼
   ┌──────────────────────────────────┐
   │         Priority Queue           │
   │  ┌────────────────────────────┐  │
   │  │  Priority 1 (high)    <────│──│ highest
   │  │  [T4][T1]                  │  │
   │  ├────────────────────────────┤  │
   │  │  Priority 2 (medium)       │  │
   │  │  [T2]                      │  │
   │  ├────────────────────────────┤  │
   │  │  Priority 3 (low)    <─────│──│ lowest
   │  │  [T3]                      │  │
   │  └────────────────────────────┘  │
   └──────────────┬───────────────────┘
                  │ dequeue (highest first)
                  ▼
       ┌────────────────────┐
       │       Worker       │
       │  takes T4 (high)   │
       └────────────────────┘

─────────────────────────────────────────
Сложность: ⭐⭐⭐
Syscalls:  fork(), msgget(), msgsnd(), msgrcv() (type = priority), msgctl()
Real world: Celery (priority), RabbitMQ priority queues, Kubernetes QoS classes
