Mini PHP-FPM (мини-пул воркеров)
   ┌────────────────────────────────────┐
   │          Master (FPM)              │
   │  fork пула · мониторинг · stats    │
   └──────┬──────────┬──────────┬───────┘
          │          │          │
   ┌──────▼───┐ ┌────▼────┐ ┌───▼─────┐
   │ Worker 1 │ │ Worker 2│ │ Worker 3│
   └──────▲───┘ └────▲────┘ └───▲─────┘
          │ accept   │ accept   │ accept
   ┌──────┴──────────┴──────────┴──────┐
   │   Слушающий сокет (ядро само      │
   │   раздаёт соединения по воркерам) │
   └───────────────────────────────────┘

   pm.max_requests: воркер обслуживает до N запросов
   и перезапускается (защита от утечек)

─────────────────────────────────────────
Сложность: ⭐⭐⭐⭐⭐
Syscalls:  socket(), bind(), listen(), accept(), fork(), waitpid(), kill()
Real world: PHP-FPM (pm.max_requests), nginx, Apache MPM prefork
